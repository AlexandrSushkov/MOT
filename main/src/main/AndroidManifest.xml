<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="dev.nelson.mot.main">

    <application
        android:name=".MotApplication"
        android:allowBackup="true"
        android:fullBackupContent="@xml/backup_descriptor"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppThemeLight">
        <activity android:name=".presentations.splash.SplashActivity"
            android:screenOrientation="portrait"
            android:theme="@style/SplashThemeLight">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".presentations.home.HomeActivity"
            android:screenOrientation="portrait"
            android:theme="@style/AppThemeLight"/>
        <activity android:name="dev.nelson.mot.presentations.payment.EditPaymentActivity"
        android:screenOrientation="portrait"
        android:theme="@style/AppThemeLight"/>
        <activity android:name="dev.nelson.mot.presentations.settings.SettingsActivity"
            android:screenOrientation="portrait"
            android:theme="@style/AppThemeLight"/>

        <activity android:name="dev.nelson.mot.main.presentations.transfer.TransferDBActivity"
            android:screenOrientation="portrait"
            android:theme="@style/AppThemeLight"/>
    </application>

</manifest>
