<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:bind="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="viewModel"
            type="dev.nelson.mot.main.presentations.payment_list.PaymentListViewModel" />

    </data>

    <androidx.coordinatorlayout.widget.CoordinatorLayout
        android:id="@+id/payment_list_coordinator_layout"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <com.google.android.material.appbar.AppBarLayout
            android:id="@+id/appbar"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:backgroundTint="@color/color_toolbar"
            app:elevation="@{viewModel.toolbarElevation}">

            <androidx.appcompat.widget.Toolbar
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:minHeight="?attr/actionBarSize"
                app:layout_scrollFlags="scroll|enterAlways|snap">

                <TextView
                    android:id="@+id/title_text"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_gravity="center"
                    android:text="Recent Payments"
                    android:textSize="16sp"
                    app:theme="@style/CustomActionBar"
                    tools:text="Title" />

            </androidx.appcompat.widget.Toolbar>

        </com.google.android.material.appbar.AppBarLayout>

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/payment_list"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"
            app:layout_behavior="@string/appbar_scrolling_view_behavior"
            app:paymentAdapter="@{viewModel.paymentAdapter}"
            tools:itemCount="8"
            tools:listitem="@layout/item_payment" />

        <ProgressBar
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            bind:isShow="@{viewModel.isLoading()}" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:text="no data yet!"
            bind:isShow="@{viewModel.isShowEmptyPlaceholder()}" />

        <com.google.android.material.floatingactionbutton.FloatingActionButton
            android:id="@+id/new_payment_fab"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_margin="@dimen/medium"
            android:backgroundTint="@color/color_fab"
            android:contentDescription="@string/add_new_payment"
            android:onClick="@{viewModel::onFabClick}"
            android:src="@drawable/ic_baseline_add_24"
            android:transitionName="new_payment"
            app:borderWidth="0dp"
            app:layout_anchor="@+id/payment_list"
            app:layout_anchorGravity="bottom|right"
            app:layout_behavior="dev.nelson.mot.main.util.recycler.fab.ScrollAwareFABBehavior" />

    </androidx.coordinatorlayout.widget.CoordinatorLayout>

</layout>
